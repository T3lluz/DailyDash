<?xml version="1.0" encoding="utf-8"?>
<!--
  Collapsed row for the live server notification.

  DecoratedCustomViewStyle leaves as little as 48dp of content height here, so it is
  two lines of native text beside one small bitmap of dials. The text uses the
  notification text appearances so it follows the shade's own light or dark theme;
  only the dials are drawn, because RemoteViews cannot draw an arc.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/server_live_title"
            style="@style/TextAppearance.Compat.Notification.Title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/server_live_summary"
            style="@style/TextAppearance.Compat.Notification.Line2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end" />
    </LinearLayout>

    <ImageView
        android:id="@+id/server_live_gauges"
        android:clipToOutline="true"
        tools:targetApi="31"
        android:layout_width="136dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        android:scaleType="fitCenter"
        android:contentDescription="@string/server_channel_live" />
</LinearLayout>
