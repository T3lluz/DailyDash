<?xml version="1.0" encoding="utf-8"?>
<!--
  Expanded live server panel. The system allows as little as 252dp here, and this
  stays well inside it.

  Words are native TextViews in the notification text appearances, so they are sharp
  at any density and follow the shade's theme. What RemoteViews cannot express — the
  dials, the history lines, the mirrored network and the per-core bars — is one bitmap
  drawn at the screen's own density (ServerLiveGraphics), and adjustViewBounds keeps
  its shape on every notification width.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingBottom="2dp">

    <TextView
        android:id="@+id/server_live_header"
        style="@style/TextAppearance.Compat.Notification.Title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/server_live_headline"
        style="@style/TextAppearance.Compat.Notification.Line2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:textStyle="bold"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/server_live_panel"
        android:clipToOutline="true"
        tools:targetApi="31"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:contentDescription="@string/server_channel_live" />

    <TextView
        android:id="@+id/server_live_facts"
        style="@style/TextAppearance.Compat.Notification.Line2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:maxLines="2"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/server_live_others"
        style="@style/TextAppearance.Compat.Notification.Info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:visibility="gone" />
</LinearLayout>
