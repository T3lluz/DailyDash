<?xml version="1.0" encoding="utf-8"?>
<!-- Placed at 4×3 (tiles, graph, list). Resizes from a 2×1 contribution strip up to 5×5;
     every size has its own layout (see GitHubWidget). -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_github_description"
    android:previewImage="@drawable/widget_preview_github"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:configure="com.macrotracker.widget.settings.WidgetSettingsActivity"
    android:widgetFeatures="reconfigurable|configuration_optional"
    android:minWidth="250dp"
    android:minHeight="180dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="4"
    android:targetCellHeight="3"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
